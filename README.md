# finmind-twse
Finmind environment for TWSE survey

# Tips:
- Auto generate commit message
```json
{
    // Chat: Generate Commit Message
    "key": "",
    "command": "github.copilot.git.generateCommitMessage",
    "when": "chatSetupCompleted && scmProvider == 'git'"
}
```
